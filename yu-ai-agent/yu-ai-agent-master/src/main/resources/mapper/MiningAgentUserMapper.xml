<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yupi.yuaiagent.mapper.MiningAgentUserMapper">
    <resultMap id="BaseResultMap" type="com.yupi.yuaiagent.domin.entity.MiningAgentUser">
        <id column="user_id" property="userId"/>
        <result column="account" property="account"/>
        <result column="password" property="password"/>
        <result column="user_name" property="userName"/>
        <result column="user_role" property="userRole"/>
        <result column="phone" property="phone"/>
        <result column="create_time" property="createTime"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <!-- 新增用户 -->
    <insert id="insert" parameterType="com.yupi.yuaiagent.domin.entity.MiningAgentUser">
        INSERT INTO agent.mining_agent_user (
            user_id, account, password, user_name, user_role, phone, create_time, is_deleted
        ) VALUES (
            #{userId}, #{account}, #{password}, #{userName}, #{userRole}, #{phone}, #{createTime}, #{isDeleted}
        )
    </insert>

    <!-- 根据ID查询用户（未删除） -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT user_id, account, password, user_name, user_role, phone, create_time, is_deleted
        FROM agent.mining_agent_user
        WHERE user_id = #{userId} AND is_deleted = FALSE
    </select>

    <!-- 根据账号查询用户（未删除） -->
    <select id="selectByAccount" resultMap="BaseResultMap">
        SELECT user_id, account, password, user_name, user_role, phone, create_time, is_deleted
        FROM agent.mining_agent_user
        WHERE account = #{account} AND is_deleted = FALSE
    </select>

    <!-- 根据角色查询用户（未删除） -->
    <select id="selectByRole" resultMap="BaseResultMap">
        SELECT user_id, account, password, user_name, user_role, phone, create_time, is_deleted
        FROM agent.mining_agent_user
        WHERE user_role = #{userRole} AND is_deleted = FALSE
        ORDER BY create_time DESC
    </select>

    <!-- 软删除用户 -->
    <update id="softDelete">
        UPDATE agent.mining_agent_user
        SET is_deleted = TRUE
        WHERE user_id = #{userId}
    </update>

    <!-- 更新用户信息 -->
    <update id="update" parameterType="com.yupi.yuaiagent.domin.entity.MiningAgentUser">
        UPDATE agent.mining_agent_user
        SET user_name = #{userName},
            user_role = #{userRole}
        WHERE user_id = #{userId}
    </update>
</mapper>